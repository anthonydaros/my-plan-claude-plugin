interface:
  display_name: "My Plan: Start"
  short_description: "Deliver one approved goal safely"
  default_prompt: "Use $my-plan:start to carry this goal through specification, implementation, review, and delivery."
policy:
  allow_implicit_invocation: false
