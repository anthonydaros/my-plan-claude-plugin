interface:
  display_name: "My Plan: Implement"
  short_description: "Build one task with the defect catalogue loaded"
  default_prompt: "Use $my-plan:implement <task> to build one task from docs/tasks/, or a plain request."
policy:
  allow_implicit_invocation: false
