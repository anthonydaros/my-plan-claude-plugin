interface:
  display_name: "My Plan: Cleanup"
  short_description: "Find dead code and confirm before removing it"
  default_prompt: "Use $my-plan:cleanup to find dead code, unused dependencies, and structural cruft in this repository. Add --fix to remove confirmed findings one at a time."
policy:
  allow_implicit_invocation: false
