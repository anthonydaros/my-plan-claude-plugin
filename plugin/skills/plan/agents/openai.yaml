interface:
  display_name: "My Plan: Plan"
  short_description: "Turn a brief into a review-ready task board"
  default_prompt: "Use $my-plan:plan to turn docs/brief.md into a plan and one task file per step."
policy:
  allow_implicit_invocation: false
